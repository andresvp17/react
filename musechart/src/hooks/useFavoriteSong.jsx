import { useState, useEffect } from 'react'
import { getFavorites, updateFavorites } from '../services/getFavorites'

const useFavoriteSong = () => {
  const [favorites, setFavorites] = useState([])
}

export default useFavoriteSong
